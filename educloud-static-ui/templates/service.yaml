apiVersion: v1
kind: Service
metadata:
  name: {{ include "educloud-static-ui.fullname" . }}
  labels:
    {{- include "educloud-static-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "educloud-static-ui.selectorLabels" . | nindent 4 }}
